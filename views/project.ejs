<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/output.css">
    <title>Project Details</title>
</head>
<body>
    <%- include('partials/navbar', { page: '' }) %>
    <div class="container mx-auto p-4">
        <h1 class="text-3xl font-bold my-4"><%= project.title %></h1>
        <p class="text-lg mb-4">
            Below, you will find detailed information about the project:
        </p>
        <img src="<%= project.feature_img_url %>" alt="<%= project.title %>" class="mb-4 rounded shadow-md">
        <p class="text-lg mb-2"><%= project.intro_short %></p>
        <p class="font-semibold text-gray-800">Impact:</p>
        <p class="mb-4 text-gray-600"><%= project.impact %></p>
        <a class="bg-green-500 text-white px-4 py-2 rounded" href="<%= project.original_source_url %>">
            More Information
        </a>
        <a class="bg-gray-500 text-white px-4 py-2 rounded ml-2" href="#" onclick="history.back(); return false;">
            Back
        </a>
        <blockquote class="mt-4 border-l-4 border-blue-500 pl-4">
            <p id="quote" class="italic text-lg"></p>
            <footer id="author" class="text-gray-700"></footer>
        </blockquote>
    </div>
    <script>
        document.addEventListener("DOMContentLoaded", () => {
            fetch("https://dummyjson.com/quotes/random")
                .then(res => res.json())
                .then(data => {
                    document.getElementById("quote").textContent = data.quote;
                    document.getElementById("author").textContent = `- ${data.author}`;
                });
        });
    </script>
</body>
</html>
